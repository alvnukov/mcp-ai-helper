/- mcp-ai-helper-task {"id":"task-022","status":"todo","title":"Make task workflow guidance explicit in config and defaults","body":"Ensure generated config guidance tells agents to batch task updates, keep statuses current, close missing tasks only intentionally, avoid many separate task calls when one batch call is enough, and prefer pipeline-integrated task status updates for each current task.","priority":"high","tags":["tasks","guidance","workflow"],"created_at":"2026-05-08T13:00:51.971307Z","updated_at":"2026-05-08T13:02:35.692932Z"} -/
namespace MCPAIHelper.Tasks

def task_022_id : String := "task-022"
def task_022_status : String := "todo"
def task_022_title : String := "Make task workflow guidance explicit in config and defaults"
def task_022_body : String := "Ensure generated config guidance tells agents to batch task updates, keep statuses current, close missing tasks only intentionally, avoid many separate task calls when one batch call is enough, and prefer pipeline-integrated task status updates for each current task."
def task_022_priority : String := "high"

end MCPAIHelper.Tasks
